place htaccess file and maintenance page inside root folder.
root folder is your project folder
in codeigniter place both the file below the application folder where application is (it is just for an example)